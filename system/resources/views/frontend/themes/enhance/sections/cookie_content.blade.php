<section class="section legal-content" aria-labelledby="cookie-content-title">
    <div class="shell legal-content__shell">
        <article class="legal-document card card-glass card-pad-lg" data-reveal="up">
            <h2 id="cookie-content-title">{{ __('Cookie usage') }}</h2>
            <p>{{ __('PixEnhance uses cookies and similar technologies to run the website, remember choices, protect accounts and understand how the product is used.') }}</p>

            <h3>{{ __('Essential cookies') }}</h3>
            <p>{{ __('Some cookies are required for sign-in, session security, fraud prevention, checkout and basic platform functionality.') }}</p>

            <h3>{{ __('Preference cookies') }}</h3>
            <p>{{ __('Preference cookies may remember settings such as interface choices, saved notices and workflow options so the product feels consistent between visits.') }}</p>

            <h3>{{ __('Analytics cookies') }}</h3>
            <p>{{ __('Analytics cookies help us measure page performance, feature usage and reliability trends without changing your core service experience.') }}</p>

            <h3>{{ __('Third-party cookies') }}</h3>
            <p>{{ __('Trusted providers may set cookies for payments, infrastructure, embedded support tools or analytics when those services are active on a page.') }}</p>

            <h3>{{ __('Managing cookies') }}</h3>
            <p>{{ __('You can control many cookies through your browser settings. Blocking some cookies may affect sign-in, billing or dashboard features.') }}</p>
        </article>
    </div>
</section>
