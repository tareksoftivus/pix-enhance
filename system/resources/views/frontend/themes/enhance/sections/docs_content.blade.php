<section class="section legal-content" aria-labelledby="docs-content-title">
    <div class="shell legal-content__shell">
        <article class="legal-document card card-glass card-pad-lg prose-enhance" data-reveal="up">
            <h2 id="docs-content-title">{{ __('Getting started') }}</h2>
            <p>{{ __('PixEnhance helps you upload an image, choose the right enhancement workflow and export a clean result for web, print or product use.') }}</p>

            <h3 id="quickstart">{{ __('Quickstart') }}</h3>
            <ol>
                <li>{{ __('Create an account or sign in to your dashboard.') }}</li>
                <li>{{ __('Upload a JPG, PNG or WebP image from your workspace.') }}</li>
                <li>{{ __('Select an enhancement mode such as upscale, restore, denoise or background cleanup.') }}</li>
                <li>{{ __('Preview the output, then download the version that fits your project.') }}</li>
            </ol>

            <h3 id="models">{{ __('Enhancement models') }}</h3>
            <p>{{ __('Use upscaling for small or compressed photos, restoration for damaged images, denoise for grainy captures and cleanup tools for commerce-ready product shots.') }}</p>

            <h3 id="sdks">{{ __('API and SDKs') }}</h3>
            <p>{{ __('Teams can connect PixEnhance to their own upload and publishing flows. Keep API credentials private, send source files over HTTPS and store returned asset URLs with your own records.') }}</p>

            <h3>{{ __('Best practices') }}</h3>
            <ul>
                <li>{{ __('Start with the highest-quality source image available.') }}</li>
                <li>{{ __('Use batch processing for repeated catalog or marketplace work.') }}</li>
                <li>{{ __('Review restored faces, text and product edges before publishing.') }}</li>
                <li>{{ __('Choose export dimensions that match the final channel instead of oversizing every asset.') }}</li>
            </ul>
        </article>
    </div>
</section>
