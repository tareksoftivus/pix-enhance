<section class="section legal-content" aria-labelledby="privacy-content-title">
    <div class="shell legal-content__shell">
        <article class="legal-document card card-glass card-pad-lg" data-reveal="up">
            <h2 id="privacy-content-title">{{ __('Privacy practices') }}</h2>
            <p>{{ __('PixEnhance collects the information needed to provide accounts, process images, secure the platform and improve product reliability.') }}</p>

            <h3>{{ __('Information we collect') }}</h3>
            <p>{{ __('We may collect account details, billing references, uploaded images, generated outputs, device information, logs and support messages you choose to send us.') }}</p>

            <h3>{{ __('How we use information') }}</h3>
            <p>{{ __('We use data to provide image enhancement, manage credits and billing, prevent abuse, troubleshoot issues, communicate service updates and improve model performance.') }}</p>

            <h3>{{ __('Image processing') }}</h3>
            <p>{{ __('Uploaded files are processed to create requested outputs. Retention periods may depend on account settings, product requirements and security needs.') }}</p>

            <h3>{{ __('Sharing and vendors') }}</h3>
            <p>{{ __('We use trusted service providers for infrastructure, payments, analytics and support. They may access only the data needed to perform those services.') }}</p>

            <h3>{{ __('Your choices') }}</h3>
            <p>{{ __('You may update account information, request deletion where available and contact support about privacy questions or data access requests.') }}</p>
        </article>
    </div>
</section>
