<?php

namespace App\Panels\{{ studlyName }}\Middleware;

use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

class {{ studlyName }}Access
{
    public function handle(Request $request, Closure $next): Response
    {
        // Add custom panel access logic here
        // For example: checking specific roles, permissions, or business rules

        return $next($request);
    }
}
